syntax = "proto3";

package lobby_response;

import "common/common.proto";

// 핸드셰이크 응답 — 서버가 발급한 sessionId 전달
message S_UDP_HELLO {
    uint64 session_id = 1;
}

// 특정 플레이어 이동 브로드캐스트
message S_LOBBY_PLAYER_MOVE {
    uint64 session_id              = 1;
    common.TransformInfo transform = 2;
}
